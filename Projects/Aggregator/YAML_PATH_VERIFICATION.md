# Aggregator.yaml 路径配置验证

## 文件位置
- **正确路径**: `/www/wwwroot/ibubble.vicp.net/Projects/Aggregator/Aggregator.yaml`
- **访问URL**: `https://home.liukun.com:8443/Projects/Aggregator/Aggregator.yaml`

## 生成配置

### 后端生成函数 (app.js 第2962-3071行)
```javascript
async function saveAggregatorYaml() {
    const outputPath = path.join(ROOT, 'Aggregator.yaml');
    fs.writeFileSync(outputPath, yamlStr);
    addLog(`✅ 自动生成配置文件: ${outputPath}`, 'success');
}
```

### 调用位置

#### 1. 服务启动时 (第3084行)
```javascript
server.listen(PORT, () => {
    startAutoUpdateJob();
    saveAggregatorYaml(); // 初始生成
});
```

#### 2. API手动生成 (第2040-2046行)
```javascript
// API: 生成/更新 Aggregator.yaml 配置文件
if (pathname === '/api/generate_yaml' && req.method === 'POST') {
    await saveAggregatorYaml();
    res.end(JSON.stringify({ success: true, message: 'Aggregator.yaml 已更新' }));
}
```

#### 3. 定时任务自动生成 (第2707-2710行)
```javascript
async function runScheduledTask() {
    // ... 执行聚合、检测等任务
    
    // Auto-generate Aggregator.yaml
    await saveAggregatorYaml();
    logEntry.details.yamlGenerated = true;
}
```

**定时任务时间**: 北京时间 00:10, 06:10, 12:10, 18:10（每6小时）

## 前端下载配置

### HTML下载链接 (index.html 第132行)
```html
<a href="Aggregator.yaml" download="Aggregator.yaml" target="_blank"
    class="px-4 py-2 rounded-lg bg-emerald-600/20 hover:bg-emerald-600/30 border border-emerald-500/30 text-emerald-400 transition flex items-center text-sm font-mono">
    <span class="material-icons-round text-base mr-2">description</span>
    Aggregator.yaml
</a>
```

**说明**: 使用相对路径 `Aggregator.yaml`，配合 `<base href="/Projects/Aggregator/">` 标签，实际访问路径为 `/Projects/Aggregator/Aggregator.yaml`

### JavaScript自动更新调用 (index.html)

#### 加载节点后自动生成 (第1057-1063行)
```javascript
// 自动更新 Aggregator.yaml 配置文件
if (allProxies.length > 0) {
    fetch('api/generate_yaml', { method: 'POST' })
        .then(r => r.json())
        .then(d => console.log('Aggregator.yaml 已自动更新'))
        .catch(e => console.warn('YAML 生成失败:', e));
}
```

#### 去重后自动生成 (第1815-1819行)
```javascript
// 去重后自动更新 Aggregator.yaml
fetch('api/generate_yaml', { method: 'POST' })
    .then(() => console.log('去重后 Aggregator.yaml 已更新'))
    .catch(e => console.warn('YAML 更新失败:', e));
```

## 验证结果

### 1. 文件存在性
```bash
$ ls -lh Projects/Aggregator/Aggregator.yaml
-rw-r--r-- 1 gemini gemini 281K Feb  8 20:03 Projects/Aggregator/Aggregator.yaml
```
✅ 文件存在且大小正常

### 2. HTTP访问
```bash
$ curl -sI https://home.liukun.com:8443/Projects/Aggregator/Aggregator.yaml
HTTP/2 200 
content-type: text/yaml
```
✅ 可以正常访问

### 3. 文件内容
```yaml
#-----------------------------------------------------------#
## 更新：2026-02-08 12:03:32
## Generator: Antigravity Aggregator
# Auto-generated by Scheduled Task
#-----------------------------------------------------------#
port: 7890
socks-port: 7891
...
```
✅ 内容格式正确

## 已清理的错误路径

### 删除的嵌套目录
- ❌ `Projects/Aggregator/Projects/Aggregator/` (已删除)
- ❌ `Projects/Aggregator/Projects/Aggregator/Aggregator.yaml` (已移动到正确位置)

### 修复前的错误配置
```javascript
// 错误的路径（已修复）
const outputPath = path.join(ROOT, 'Projects', 'Aggregator', 'Aggregator.yaml');
```

## 总结

✅ **所有生成路径已统一**: `path.join(ROOT, 'Aggregator.yaml')`
✅ **定时任务配置正确**: 每6小时自动生成
✅ **前端下载链接正确**: 相对路径配合base标签
✅ **API调用正确**: `/api/generate_yaml`
✅ **错误的嵌套目录已清理**: `Projects/Aggregator/Projects/` 已删除

**结论**: Aggregator.yaml 的生成、存储、访问路径已完全统一和修复。
